# Postgres credentials.
#
# This is a devnet-only password; do not reuse for hosted environments.
# Override at apply time with `kubectl create secret generic postgres-credentials
# --from-literal=POSTGRES_PASSWORD=...` or via kustomize secretGenerator.
#
# Canonical: docs/technical/full-stack-devnet.md
# Issue: #146.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: robotmoney
type: Opaque
stringData:
  POSTGRES_PASSWORD: "rmoney_dev"
