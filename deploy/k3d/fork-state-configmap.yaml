# Fork-state fixture for the in-cluster Anvil.
#
# Placeholder — `scripts/devnet/k3d-up.sh` overrides this ConfigMap at
# apply time with the real contents of
# `testing/fixtures/fork-state/CURRENT.anvil-state`. This file exists so
# `kubectl kustomize` and `kubectl apply --dry-run=client -k` are
# self-contained; the placeholder data is never actually consumed.
#
# Canonical: docs/technical/full-stack-devnet.md §"Fork-state fixture"
# Issue: #146.
apiVersion: v1
kind: ConfigMap
metadata:
  name: fork-state
  namespace: robotmoney
data:
  # Placeholder — replaced by k3d-up.sh with the real anvil state JSON.
  CURRENT.anvil-state: |
    {"placeholder": true}
